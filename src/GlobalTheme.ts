import { createTheme, Theme, useMediaQuery } from "@mui/material";

export const defaultTheme: Theme = createTheme({
  palette: {
    primary: {
      main: "#962BB0",
      light: "#DEBEE7",
      dark: "#8928AA",
      contrastText: "#fff",
    },
  },
});

export const theme1: Theme = createTheme({
  palette: {
    primary: {
      main: "#962BB0",
      light: "#DEBEE7",
      dark: "#8928AA",
      contrastText: "#fff",
    },
  },
});

export const theme2: Theme = createTheme({
  palette: {
    primary: {
      main: "#4552B4",
      light: "#C6CAE9",
      dark: "#3E4AAA",
      contrastText: "#fff",
    },
  },
});

export const theme3: Theme = createTheme({
  palette: {
    primary: {
      main: "#E9463D",
      light: "#FBCDD3",
      dark: "#DB3C3C",
      contrastText: "#fff",
    },
  },
});
